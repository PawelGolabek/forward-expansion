event_inherited()

noEyes = true;

function initiate2(){
	/*
	dzida = instance_create_depth(x + sprite_width/2,y - 64 * image_yscale,depth-10,o_dzida);
	dzida.image_xscale = self.image_xscale
	dzida.image_yscale = self.image_yscale
	dzida.x -= rPupil.sprite_width/2
	dzida.y -= rPupil.sprite_height/2
	dzida.originX = dzida.x - x;
	dzida.originY = dzida.y - y;
	dzida.unit = unit;
	dzida.owner = self;
*/
	image_xscale = 4;
	image_yscale = 4;

}

